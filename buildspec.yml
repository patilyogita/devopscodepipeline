version:0.2

phases:
  install:
    runtime-versions:
      java: corretto11
pre_build:
  commands:
     - echo Nothing to do in the pre_build
 build:
   commands:
     - echo Build strating date
 post_build:
   commands:
    - echo Build completed on date
 artifacts:
 files:
 - '**/*'
